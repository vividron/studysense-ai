const Loader = () => {
    return (
        <div className="min-h-screen flex items-center justify-center">
            <div
                className="w-7 h-7 border-3 rounded-full border-b-white/20 border-(--primary) animate-spin"
            />
        </div>
    );
};

export default Loader;
